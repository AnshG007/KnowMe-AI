[project]
name = "chatbot-project"
version = "0.1.0"
description = "Personal AI Chatbot"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "groq",
    "pydantic",
    "python-dotenv",
    "uvicorn"
]
